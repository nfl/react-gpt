run_on: &agents
  - Env=dev

steps:
  - label: "Run tests"
    command:
      - npm install
      - npm run lint
      - CI=false npm test
    artifact_paths:
      - "./build/**/*"
      - "./coverage/*"
    agents: *agents
